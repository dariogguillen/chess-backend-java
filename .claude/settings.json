{
  "permissions": {
    "allow": [
      "Bash(./mvnw *)",
      "Bash(./init.sh)",
      "Bash(jq *)",
      "Bash(git *)",
      "Bash(docker *)",
      "Bash(docker compose *)"
    ],
    "deny": [
      "Bash(rm -rf *)"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'path=\"$CLAUDE_TOOL_INPUT_FILE_PATH\"; if [[ \"$path\" == *\"/db/migration/V\"* ]] && [[ -f \"$path\" ]]; then echo \"BLOCKED: Flyway migration files that already exist are forward-only. Add a new migration instead of editing $path.\" >&2; exit 1; fi; exit 0'"
          }
        ]
      }
    ]
  }
}
