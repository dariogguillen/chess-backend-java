# Current session

**Status:** No active session.

To start one, the `leader` should:

1. Read `AGENTS.md` and this file.
2. Read `feature_list.json` and pick the pending feature with the
   lowest `priority`.
3. Mark that feature as `in_progress` in `feature_list.json`.
4. Replace the content of this file with a plan for the feature.
5. Wait for user approval.

A plan should contain:

- Feature ID and title.
- Approach in 2-3 sentences.
- Files to create or modify.
- Verification approach (tests, endpoints exercised).
- Java / Spring concepts to highlight in the feature note.
